/**
 * @param {string} tiles
 * @return {number}
 */
var numTilePossibilities = function (tiles) {
  let count = 0;
  let hash = {};
  // swap position
  for (let i = 0; i < tiles.length; i++) {
    for (let j = i + 1; j < array.length; j++) {
      const element = array[j];
    }
  }
  return count;
};

console.log(numTilePossibilities("ABBDCE"));
